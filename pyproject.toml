[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "agmarknet-api"
version = "0.1.0"
description = "Agmarknet API package"
readme = "README.md"
requires-python = ">=3.8"
dependencies = []
